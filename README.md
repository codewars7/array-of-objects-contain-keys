# Check if array of objects contains certain key

### To run this app
clone it :
```
git@github.com:codewars7/array-of-objects-contain-keys.git
```

and run a command in your terminal
```
node index.js
```
